# TB CLASS — AI 활용과정 강의 웹페이지 설계

> 생성형 AI 활용 실무 · 4주 과정
> 대상: 사회복지 전공 대학원생(50대 중심) · AI 입문
> 참조 UX/UI: `/Users/toolb/dev/dev/tbacademy` (AICREW 아카데미)

---

## 0. 설계 원칙 — "50대가 혼자 봐도 안 막힌다"

강의 대본의 진행 원칙(`새 화면은 같이 한 칸씩`, `용어는 쉽게`, `설명→예시→바로 실습`)을 그대로 화면 규칙으로 옮긴다.

| 원칙 | 화면에서의 구현 |
|---|---|
| 한 화면에 하나만 | 슬라이드 1장 = 개념 1개. 여러 개 나열 금지 |
| 손가락은 위아래로만 | 기본 이동은 세로 스크롤 + 하단 큰 `다음` 버튼 |
| 글씨는 크게 | 본문 18px 시작, `글씨 크기` 3단계 토글 제공 |
| 버튼은 손가락만큼 | 주요 버튼 높이 56px(h-14) 이상, 아이콘 옆에 항상 한글 라벨 |
| 영어·전문용어 최소화 | 화면 문구는 한국어 우선. `LLM` 같은 단어는 반드시 옆에 비유 |
| 실수해도 되돌아온다 | 마지막으로 본 슬라이드 저장 → "이어서 보기" 배너 |
| 복사는 한 번에 | 프롬프트는 `복사하기` 버튼 하나 + 성공 안내 문구 |

---

## 1. 기술 스택 (tbacademy와 동일 계열)

- **Next.js 16 (App Router) + React 19**
- **Tailwind CSS v4** (`@theme inline` 토큰 방식 그대로)
- **shadcn/ui + radix-ui + lucide-react** — `button`, `card`, `badge`, `separator`, `avatar`, `sheet`(목차용) 재사용
- **Paperlogy 폰트** — `tbacademy/public/fonts` 9종 그대로 복사, `next/font/local`
- 콘텐츠는 **MDX/velite 없이 TypeScript 데이터 파일**로 관리 (슬라이드는 구조화 데이터가 유리)
- 배포는 tbacademy와 동일하게 `@opennextjs/cloudflare` (나중 단계)

tbacademy에서 **가져올 것**: 디자인 토큰 구조, 폰트, `cn()`, Button/Card/Badge, `SectionLabel` 감각, 풀블리드 섹션 패딩(`px-4 sm:px-8 lg:px-12`).
tbacademy에서 **버릴 것**: 다크 테마(next-themes), 초대형 영어 대문자 헤드라인, 배경 orb/grid 장식 남발, 드롭다운 메가 네비.

---

## 2. 디자인 시스템 — 화이트 · 시니어 가독성

### 2.1 컬러 (light only)

tbacademy의 웜 오렌지 브랜드를 유지하되, 회색 대비를 한 단계 진하게 올린다.

```css
:root {
  --radius: 0.75rem;

  --background: oklch(1 0 0);            /* 순백 */
  --foreground: oklch(0.18 0 0);         /* 본문 — 거의 검정 */
  --card: oklch(1 0 0);
  --muted: oklch(0.97 0 0);
  --muted-foreground: oklch(0.38 0 0);   /* tbacademy 0.42 → 0.38 (대비 ↑) */
  --border: oklch(0.82 0 0);             /* 경계 뚜렷하게 */

  --primary: oklch(0.58 0.19 50);        /* 브랜드 오렌지 (본문 위 대비 확보) */
  --primary-foreground: oklch(0.99 0 0);
  --accent: oklch(0.45 0.05 250);        /* 슬레이트 — 보조 정보 */

  /* 슬라이드 의미 색 — 딱 4개만 */
  --tone-concept: oklch(0.45 0.05 250);  /* 개념·설명 */
  --tone-practice: oklch(0.58 0.19 50);  /* 같이 해봐요(실습) */
  --tone-warning: oklch(0.52 0.20 30);   /* 주의(환각 등) */
  --tone-done: oklch(0.50 0.15 150);     /* 완료·정리 */
}
```

- 다크 모드 없음. `ThemeToggle` 제거. 헤더가 흰색으로 고정되어 프로젝터에서도 눈부심 대신 명료함.
- 색만으로 정보를 전달하지 않는다 — 항상 아이콘 + 라벨 텍스트 동반.

### 2.2 타이포그래피

```
기본 배율(html font-size): 보통 18px / 크게 20px / 아주 크게 22px  ← 사용자 토글
본문           1rem, line-height 1.8
슬라이드 본문   1.25rem (모바일) ~ 1.5rem (데스크톱)
슬라이드 제목   clamp(1.75rem, 5vw, 3rem), font-weight 800
페이지 h1      clamp(2rem, 6vw, 3.5rem), font-weight 900
자간           한글은 tracking -0.01em까지만 (tbacademy의 -0.04em은 한글 가독성 저하)
```

- 모든 크기는 `rem` — 브라우저 200% 확대에서도 레이아웃이 깨지지 않게 고정 높이 지양.
- 한 줄 최대 길이 `max-w-[38rem]` (한글 기준 눈이 편한 폭).

### 2.3 컴포넌트 규격

- **버튼**: `xl` 사이즈를 추가 (`h-14 px-6 text-lg rounded-xl`). 슬라이드 이동·복사 버튼은 전부 xl.
- **카드**: `border-2 border-border rounded-2xl` — 얇은 선은 50대 화면에서 잘 안 보인다.
- **포커스**: `ring-3 ring-primary/50` 유지 (키보드 사용자·화면 확대 사용자에게 중요).
- **터치 타깃**: 모든 클릭 요소 최소 48×48px.

---

## 3. 사이트 구조

```
/                     홈 — 과정 소개, 4주 커리큘럼, 오늘 강의 시작 버튼
/week/1               1주차 강의 (슬라이드 덱)   ← 핵심
/week/2 · 3 · 4       준비 중 (뼈대만)
/prompts              프롬프트 모음 — 주차별·용도별 복사 카드 전체
/instructor           강사 소개
/help                 도와주세요 — 가입이 안 될 때, 화면이 영어일 때 등
```

**헤더(모바일 우선)**: 로고 + `강의` `프롬프트` `강사 소개` `도움말` 4개만. 드롭다운 없음.
**모바일 하단 고정 바**(슬라이드 페이지에서만): `☰ 목차` · `다음 ▼` · `📋 프롬프트` 3버튼, `env(safe-area-inset-bottom)` 처리.

---

## 4. 슬라이드 시스템 (핵심)

### 4.1 화면 동작 — 하이브리드

```
┌──────────────────────────────┐
│ 1주차 · AI와 처음 만나기      │  ← sticky 상단
│ ▰▰▰▱▱▱▱▱▱▱  3 / 14   [목차]  │     진행바 + 목차 버튼
├──────────────────────────────┤
│                              │
│   ② 생각보다 쉽습니다         │  ← 슬라이드 1장 = min-h-[78svh]
│                              │     스크롤 스냅(proximity)
│   AI는 '말이 통하는           │
│   아주 똑똑한 조수'입니다     │
│                              │
│   [ 비유 카드 ]               │
│                              │
├──────────────────────────────┤
│  ◀ 이전        다음 ▼         │  ← sticky 하단 (h-14)
└──────────────────────────────┘
         ↓ 그냥 스크롤해도 다음 장
```

- **세로 스크롤 + 스냅**: `snap-y snap-proximity` — 강제 스냅이 아니라 근접 스냅이라 답답하지 않다.
- **현재 위치 추적**: `IntersectionObserver`로 진행바/목차 하이라이트 동기화.
- **키보드**: `↓ / Space / →` 다음, `↑ / ←` 이전, `F` 전체화면. (강사 노트북용)
- **목차 시트**: 슬라이드 제목 14개 리스트 → 탭하면 해당 위치로 스크롤.
- **이어보기**: 마지막 본 인덱스를 `localStorage`에 저장 → 재방문 시 상단에 `지난번 5번째 장에서 멈췄어요 [이어서 보기]` 배너.
- **프로젝터 모드**: 강사가 `F` 또는 상단 버튼 → 1화면 1슬라이드 고정, 폰트 1.3배, 좌우 키 이동, 상단 바 숨김.

### 4.2 슬라이드 데이터 모델

```ts
// src/lib/deck-types.ts
type Tone = "concept" | "practice" | "warning" | "done";

type TeacherNote = {
  time?: string;        // "10~30분"
  say?: string[];       // 이렇게 말해요 (멘트)
  tip?: string[];       // 진행 팁
  watch?: string[];     // 자주 막히는 지점
};

type Slide =
  | { type: "cover";     title; subtitle; meta: string[] }
  | { type: "goal";      title; items: { icon; text }[] }        // 오늘의 목표 체크
  | { type: "point";     title; lead; body?; figure? }           // 개념 한 문장
  | { type: "analogy";   title; left; right; caption }           // "이건 이런 겁니다"
  | { type: "compare";   title; columns; rows }                  // 도구 비교표
  | { type: "steps";     title; steps: { label; detail?; image? }[] }  // 따라하기
  | { type: "prompt";    title; intro?; prompts: PromptItem[] }  // 복사 카드
  | { type: "warning";   title; lead; points: string[] }         // 환각 주의
  | { type: "summary";   title; lines: string[] }                // 한 줄 요약
  | { type: "next";      title; lead; items: string[] };         // 다음주 예고

type SlideBase = { id: string; nav: string; tone: Tone; notes?: TeacherNote };
```

각 슬라이드 타입마다 전용 렌더 컴포넌트 1개. 데이터만 채우면 화면이 나온다 → 2~4주차는 데이터 파일만 추가.

### 4.3 반응형 규칙

| 요소 | 모바일(<768px) | 데스크톱 |
|---|---|---|
| 비교표 `compare` | **카드 스택**으로 자동 변환 (도구 1개 = 카드 1장) | 표 |
| 따라하기 `steps` | 세로 번호 리스트 + 큰 스크린샷 | 2열 (번호 / 설명) |
| 프롬프트 카드 | 풀폭, 복사 버튼 풀폭 | 최대 42rem |
| 하단 네비 | 고정 바 3버튼 | 슬라이드 하단 인라인 |

---

## 5. 프롬프트 복사 카드 (요구사항 핵심)

```
┌────────────────────────────────────┐
│ 📋 실습 1 · 나를 소개해달라고 하기   │
│                                    │
│  안녕? 나는 [ 사회복지 ]를          │  ← 빈칸은 노란 배경,
│  공부하는 대학원생이야.             │     탭하면 바로 수정 가능
│  내 소개를 정중하게 3줄로 써줘.     │
│                                    │
│  ┌──────────────────────────────┐  │
│  │      📋  복사하기             │  │  ← h-14 풀폭
│  └──────────────────────────────┘  │
│  ChatGPT 열기 →                    │
└────────────────────────────────────┘
   누르면 ↓
   ✅ 복사했어요! ChatGPT 입력창에 붙여넣으세요
```

- **빈칸 채우기**: 템플릿에 `{{주제}}` 표기 → 노란 하이라이트 인풋으로 렌더. 사용자가 고친 값이 복사 결과에 반영된다. ("빈칸만 바꾸세요"가 50대에게 가장 쉬운 프롬프트 학습법 — 대본의 '복붙 카드' 방식 그대로.)
- **복사 UX**: `navigator.clipboard` → 실패 시 `textarea + execCommand` 폴백 → 그래도 실패하면 "길게 눌러서 복사하세요" 안내로 전환. iOS Safari 구버전 대비.
- **피드백**: 버튼이 `✅ 복사했어요`로 2초간 변경 + 하단 토스트. 소리/진동 없음.
- **재사용**: 슬라이드 안 `prompt` 타입과 `/prompts` 페이지가 **같은 컴포넌트**를 쓴다. 데이터는 주차 파일에서 자동 수집.
- `/prompts` 페이지: `1주차 / 2주차 …` 탭 + `글쓰기 · 요약 · 보고서` 용도 뱃지. 강의 후 복습용 단일 페이지.

---

## 6. 강사 모드 (대본 숨김)

- 헤더 우측 끝 작은 자물쇠 아이콘 → 켜면 `localStorage.teacherMode = true` (`?teacher=1`로도 진입).
- 켜졌을 때만 각 슬라이드 하단에 노트 패널이 붙는다:

```
┌ 강사 노트 ─────────────────────┐
│ ⏱ 10~30분                      │
│ 🗣 "여러분, AI 어렵지 않습니다…" │
│ 💡 익숙한 것(스마트폰 검색)에서  │
│    출발하면 문턱이 낮아집니다    │
│ ⚠️ 구글 비밀번호 잊은 분 대비    │
└────────────────────────────────┘
```

- 상단에 **0~180분 타임라인 바** 추가 표시 — 지금 몇 분짜리 구간인지 강사만 보임.
- 대본 하단 총평(`시간은 반 정도만 채운다는 마음으로…`)은 덱 마지막 강사 노트에 배치.
- 수강생 화면에는 어떤 흔적도 남지 않는다 (SSR 아닌 클라이언트 조건부 렌더).

---

## 7. 1주차 슬라이드 구성안 (대본 → 14장)

| # | 제목 | 타입 | 대본 구간 |
|---|---|---|---|
| 1 | 1주차 · AI와 처음 만나기 | cover | 표지 (3시간) |
| 2 | 오늘 이후에 여러분은 이렇게 변합니다 | goal | 이 시간의 목표 ①②③ |
| 3 | AI는 '말이 통하는 똑똑한 조수' | point | ① 오프닝 |
| 4 | LLM = 엄청나게 많은 책을 읽은 사람 | analogy | ② 개념 |
| 5 | 생성형 AI = 그 지식으로 새로 만드는 것 | point | ② 개념 |
| 6 | ⚠️ 딱 하나 조심 — 환각 | warning | ② 한계 |
| 7 | 도구가 왜 여러 개일까? (한식·중식·일식) | analogy | ③ 도입 |
| 8 | 도구 4가지, 뭘 잘하나 | compare | ③ 비교표 |
| 9 | 한 줄로 외우세요 | summary | ③ 한 줄 요약 |
| 10 | 같이 해봐요 — 가입하기 | steps | ④ 가입·로그인 4단계 |
| 11 | 첫 대화 걸어보기 | prompt | ⑤ 실습 1·2 |
| 12 | 좋은 질문의 비밀 3가지 | point+예시 | ⑥ 역할·조건·예시 |
| 13 | 보고서 한 편 만들기 | prompt | ⑦ 1~4단계 복붙 카드 |
| 14 | 오늘 정리 + 다음 주 예고 | next | ⑧ 마무리 |

- 12번은 **같은 질문 / 좋은 질문** 비교를 좌우(모바일 상하) 카드로 보여줘 차이를 눈으로 체감하게 한다.
- 13번은 4개 프롬프트가 세로로 이어지고, 각 단계에 `여기까지만 해도 성공이에요` 체크 표시(대본의 "2단계까지만 해도 성공" 반영).

**2~4주차 뼈대**: 카드에 `준비 중` 뱃지 + 예고 문구만. 2주차는 대본에 예고가 있으니 `자료 모으기 + 진짜인지 검증하기 · NotebookLM`으로 제목 확정.

---

## 8. 강사 소개 페이지 `/instructor`

tbacademy의 `instructor-card` / `avatar-block` 감각을 화이트·큰 글씨로 재구성.

```
[ 사진 (원형 160px) ]
이름 · 직함
"AI를 처음 켜는 순간부터 함께합니다" ← 한 줄 소개

▸ 이런 걸 가르칩니다   (3~4개 큰 글머리)
▸ 걸어온 길            (연도 + 한 줄 타임라인)
▸ 강의 이력            (기관 로고/이름 리스트)
▸ 궁금한 게 있으면      (단톡방 · 이메일 · 전화 — 큰 버튼)
```

50대 수강생은 **신뢰 정보(경력·소속·강의 이력)**를 먼저 본다. 화려한 포트폴리오보다 이 블록을 상단에.

---

## 9. `/help` 도와주세요

대본의 "자주 막히는 지점"을 그대로 수강생용 FAQ로 변환. 아코디언 대신 **전부 펼쳐진 큰 카드**(접힌 UI는 못 찾는다).

- 구글 계정 비밀번호가 기억 안 나요
- 인증 문자가 안 와요 → 스팸함·문자함 확인
- 화면이 영어로 떠요 → 한국어로 물어보면 한국어로 답해요
- 타자 치기 힘들어요 → 마이크 버튼 쓰는 법
- 만든 보고서를 어떻게 저장하나요

---

## 10. 파일 구조 (실제 구현)

```
tbclass/
├─ src/
│  ├─ app/
│  │  ├─ layout.tsx              # Paperlogy, light 고정, PrefsProvider, Header/Footer
│  │  ├─ globals.css             # 토큰 + 시니어 타이포
│  │  ├─ icon.svg                # 파비콘
│  │  ├─ page.tsx                # 홈
│  │  ├─ week/[week]/page.tsx    # 슬라이드 덱 (준비 중 주차는 안내 화면)
│  │  ├─ prompts/page.tsx
│  │  ├─ instructor/page.tsx
│  │  └─ help/page.tsx
│  ├─ components/
│  │  ├─ ui/{button,card}.tsx    # button 에 xl(h-16) 사이즈 추가
│  │  ├─ site/{header,footer,font-scale-toggle,teacher-toggle}.tsx
│  │  ├─ deck/
│  │  │  ├─ deck.tsx             # 스크롤·키보드·진행바·목차·이어보기·프로젝터
│  │  │  ├─ slide-frame.tsx      # 슬라이드 공통 틀 (톤 뱃지 + 번호 + 강사 노트)
│  │  │  ├─ slide-view.tsx       # 타입 → 렌더러 분기
│  │  │  ├─ teacher-notes.tsx
│  │  │  └─ slides/{basic,practice}.tsx   # 렌더러 11종
│  │  └─ prompt/prompt-card.tsx  # 빈칸 편집 + 복사 + 폴백 (한 파일)
│  ├─ content/{course,week-1,instructor,help}.ts
│  └─ lib/{deck-types,prefs,use-copy,utils}.ts
├─ scripts/
│  ├─ check-layout.mjs           # 화면 캡처 + 가로 넘침·콘솔 오류 점검
│  └─ check-behavior.mjs         # 복사·이동·목차·강사모드·글씨크기 동작 점검
└─ public/fonts/                 # tbacademy Paperlogy 9종
```

설계안과 달라진 점 — 슬라이드 렌더러는 타입별 파일 대신 `basic.tsx`(설명형 7종) /
`practice.tsx`(실습형 4종) 두 파일로 묶었다. 한 렌더러가 30~50줄이라 파일을 11개로
쪼개면 오히려 찾기 어렵다. `copy-button` / `blank-input` 도 `prompt-card.tsx` 안에
두었다.

---

## 11. 구현 상태

전부 구현 완료. `pnpm dev` 로 확인 가능.

| 항목 | 상태 |
|---|---|
| Next.js 16 + Tailwind v4 + Paperlogy, 화이트 테마 토큰 | ✅ |
| 1주차 14장 (대본 전문 반영, 강사 노트 포함) | ✅ |
| 슬라이드 렌더러 11종 (cover/goal/point/analogy/warning/compare/steps/prompt/rules/summary/next) | ✅ |
| 덱 컨트롤러 — 진행바·목차·이전/다음·키보드·이어보기·프로젝터(전체화면) | ✅ |
| 프롬프트 카드 — 빈칸 편집·복사·폴백·ChatGPT 바로 열기 | ✅ |
| `/prompts` 모음, `/instructor`, `/help`, 홈 | ✅ |
| 강사 모드 토글, 글씨 크기 3단계 | ✅ |
| 모바일 검증 (390px 기준 가로 넘침 0, 콘솔 오류 0) | ✅ |
| 2~4주차 | 뼈대만 (`준비 중` 안내 화면) |
| Cloudflare 배포 설정 | 미적용 |

구현 중 실제로 잡은 문제 (기록):

- 스크롤 스냅이 로드 직후 131px 자동 스크롤을 유발해 &lsquo;이어보기&rsquo; 배너를 가림 → **스냅 제거**, 정렬은 버튼이 담당.
- 헤더 높이가 모바일(134px)/데스크톱(92px)에서 달라 진행바가 겹침 → 런타임 측정 후 `--deck-offset` 주입.
- 빈칸 입력 폭을 `ch` 로 잡으니 한글이 잘림 → 한글 1em·라틴 0.55em 가중 계산.
- 목차에서 먼 장으로 갈 때 부드러운 스크롤이 수천 픽셀을 훑어 느리고 어지러움 → 멀리 뛸 때는 즉시 이동.
- 발표 화면에서 **덱 컨테이너만** 전체화면으로 띄우니 사이트 헤더는 사라졌는데 진행바는 여전히 `top: 헤더높이`에 붙어 빈 띠가 생기고 슬라이드 상단이 잘림 → **문서 전체를 전체화면**으로 띄우고 헤더·푸터는 CSS로 숨김, 오프셋은 `projector` 상태가 바뀔 때 다시 측정.
- 발표 화면에서 본문 폭이 864px에 묶임 → `max-w-3xl`(utilities 레이어)을 이기려면 `@layer` **밖**에 규칙을 둬야 함. 기준 글자도 22px로 올려 rem 기반 크기가 전부 함께 커지게 함.
- 발표 화면에 강사 대본이 그대로 투사됨 → 발표 화면 = 수강생이 보는 화면이므로 대본은 숨김.

---

## 12. 결정 필요 / 나중에 채울 것

- 강사 프로필 내용(이름·경력·사진·연락처) — 실제 정보 필요
- 브랜드 로고·과정명 표기 (`TB CLASS`? 주관 기관명?)
- 가입 절차 스크린샷 4장 (10번 슬라이드용) — 없으면 아이콘 + 텍스트로 시작
- 2~4주차 대본 — 나오는 대로 `week-2.ts` 형태로 추가
- 단톡방 링크 / 문의 연락처
